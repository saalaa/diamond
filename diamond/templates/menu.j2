<div class="actions">
  <hr>

  <p>
    {% if page.id %}
      {{ page.ymd_hm or _('never') }} –

      <a href="/{{ page.slug }}?version={{ page.id }}" class="wikilink">
        {{ page.id }}{{ '*' if page.active else '' }}</a> |

      <a href="/history/{{ page.slug }}">{{ _('History') }}</a> |
    {% endif %}

    {% if page.id %}
      <a href="/edit/{{ page.slug }}">{{ _('Edit') }}</a>
    {% else %}
      <a href="/edit/{{ page.slug }}">{{ _('Create') }}</a>
    {% endif %}

    {% if page.id and current_user.admin %}
      {% if page.active %}
        | <a href="/deactivate/{{ page.slug }}">
          {{ _('Deactivate') }}</a>
      {% else %}
        | <a href="/activate/{{ page.slug }}?version={{ page.id}}">
          {{ _('Activate') }}</a>
      {% endif %}
    {% endif %}
  </p>
</div>
